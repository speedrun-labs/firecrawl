# TODO

- [ ] Add optional auto-scroll to Playwright service to capture lazy-loaded content (scroll to bottom before capturing HTML, triggering scroll-based lazy loading for product cards, images, etc.)
